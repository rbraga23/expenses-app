version: '3.9'

services:
  mysql:
    image: mysql:5.7
    container_name: mysql-onfly
    environment:
      MYSQL_ROOT_PASSWORD: onfly
      MYSQL_DATABASE: onfly
      MYSQL_TCP_PORT: 3306
    ports:
      - '3306:3306'
    volumes:
      - ./db:/var/lib/mysql
    restart: always
